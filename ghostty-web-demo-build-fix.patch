diff --git a/demo/colors-demo.html b/demo/colors-demo.html
index 38bbdd9..e7955d9 100644
--- a/demo/colors-demo.html
+++ b/demo/colors-demo.html
@@ -153,8 +153,7 @@
     </div>
 
     <script type="module">
-      import { init, Terminal } from '../lib/index.ts';
-      import { FitAddon } from '../lib/addons/fit.ts';
+      import { init, Terminal, FitAddon } from '/dist/ghostty-web.js';
 
       let term;
       let fitAddon;
diff --git a/demo/index.html b/demo/index.html
index 2a0d323..46ec761 100644
--- a/demo/index.html
+++ b/demo/index.html
@@ -131,8 +131,7 @@
     </div>
 
     <script type="module">
-      import { init, Terminal } from '../lib/index.ts';
-      import { FitAddon } from '../lib/addons/fit.ts';
+      import { init, Terminal, FitAddon } from '/dist/ghostty-web.js';
 
       let term;
       let ws;
diff --git a/demo/scrollbar-test.html b/demo/scrollbar-test.html
index 556598e..d3931da 100644
--- a/demo/scrollbar-test.html
+++ b/demo/scrollbar-test.html
@@ -35,8 +35,7 @@
     <div id="terminal"></div>
 
     <script type="module">
-      import { init, Terminal } from '../lib/index.ts';
-      import { FitAddon } from '../lib/addons/fit.ts';
+      import { init, Terminal, FitAddon } from '/dist/ghostty-web.js';
 
       await init();
 
diff --git a/scripts/build-wasm.sh b/scripts/build-wasm.sh
index 53014fd..e678655 100755
--- a/scripts/build-wasm.sh
+++ b/scripts/build-wasm.sh
@@ -2,21 +2,54 @@
 set -euo pipefail
 
 echo "🔨 Building ghostty-vt.wasm..."
+PROJECT_ROOT=$(pwd)
 
 # Check for Zig
-if ! command -v zig &> /dev/null; then
-    echo "❌ Error: Zig not found"
+REQUIRED_ZIG_VERSION="0.15.2"
+ZIG_CMD=()
+
+if command -v zig &> /dev/null && [ "$(zig version)" = "$REQUIRED_ZIG_VERSION" ]; then
+    ZIG_CMD=(zig)
+elif command -v mise &> /dev/null && mise x zig@"$REQUIRED_ZIG_VERSION" -- zig version &> /dev/null; then
+    ZIG_CMD=(mise x zig@"$REQUIRED_ZIG_VERSION" -- zig)
+else
+    echo "❌ Error: Zig $REQUIRED_ZIG_VERSION not found"
     echo ""
-    echo "Install Zig 0.15.2+:"
-    echo "  macOS:   brew install zig"
-    echo "  Linux:   https://ziglang.org/download/"
+    echo "Install Zig $REQUIRED_ZIG_VERSION or make it available through mise:"
+    echo "  mise install zig@$REQUIRED_ZIG_VERSION"
     echo ""
     exit 1
 fi
 
-ZIG_VERSION=$(zig version)
+ZIG_VERSION=$("${ZIG_CMD[@]}" version)
 echo "✓ Found Zig $ZIG_VERSION"
 
+run_zig() {
+    if [ "$(uname -s)" = "Darwin" ] && [ -d "/Library/Developer/CommandLineTools/SDKs/MacOSX15.4.sdk" ]; then
+        local developer_dir
+        developer_dir=$(mktemp -d)
+        mkdir -p "$developer_dir/usr/bin"
+        cat > "$developer_dir/usr/bin/xcrun" <<'EOF'
+#!/bin/sh
+case "$*" in
+  *--show-sdk-path*) echo /Library/Developer/CommandLineTools/SDKs/MacOSX15.4.sdk ;;
+  *) DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer exec /usr/bin/xcrun "$@" ;;
+esac
+EOF
+        chmod +x "$developer_dir/usr/bin/xcrun"
+
+        set +e
+        DEVELOPER_DIR="$developer_dir" "${ZIG_CMD[@]}" "$@"
+        local zig_status=$?
+        set -e
+
+        rm -rf "$developer_dir"
+        return "$zig_status"
+    fi
+
+    "${ZIG_CMD[@]}" "$@"
+}
+
 # Initialize/update submodule
 if [ ! -d "ghostty/.git" ]; then
     echo "📦 Initializing Ghostty submodule..."
@@ -28,29 +61,40 @@ fi
 # Apply patch
 echo "🔧 Applying WASM API patch..."
 cd ghostty
-git apply --check ../patches/ghostty-wasm-api.patch || {
+PATCH_APPLIED_BY_SCRIPT=0
+
+cleanup_patch() {
+    if [ "$PATCH_APPLIED_BY_SCRIPT" -eq 1 ]; then
+        echo "🧹 Cleaning up..."
+        (
+            cd "$PROJECT_ROOT/ghostty"
+            git apply -R "$PROJECT_ROOT/patches/ghostty-wasm-api.patch"
+            # Remove new files created by the patch
+            rm -f include/ghostty/vt/terminal.h
+            rm -f src/terminal/c/terminal.zig
+        )
+    fi
+}
+trap cleanup_patch EXIT
+
+if git apply --check ../patches/ghostty-wasm-api.patch 2> /dev/null; then
+    git apply ../patches/ghostty-wasm-api.patch
+    PATCH_APPLIED_BY_SCRIPT=1
+elif git apply -R --check ../patches/ghostty-wasm-api.patch 2> /dev/null; then
+    echo "✓ WASM API patch already applied"
+else
     echo "❌ Patch doesn't apply cleanly"
     echo "Ghostty may have changed. Check patches/ghostty-wasm-api.patch"
     exit 1
-}
-git apply ../patches/ghostty-wasm-api.patch
+fi
 
 # Build WASM
 echo "⚙️  Building WASM (takes ~20 seconds)..."
-zig build lib-vt -Dtarget=wasm32-freestanding -Doptimize=ReleaseSmall
+run_zig build lib-vt -Dtarget=wasm32-freestanding -Doptimize=ReleaseSmall
 
 # Copy to project root
 cd ..
 cp ghostty/zig-out/bin/ghostty-vt.wasm ./
 
-# Revert patch to keep submodule clean
-echo "🧹 Cleaning up..."
-cd ghostty
-git apply -R ../patches/ghostty-wasm-api.patch
-# Remove new files created by the patch
-rm -f include/ghostty/vt/terminal.h
-rm -f src/terminal/c/terminal.zig
-cd ..
-
 SIZE=$(du -h ghostty-vt.wasm | cut -f1)
 echo "✅ Built ghostty-vt.wasm ($SIZE)"
